extend type Query {
  product(id: ID!): Product
}

type Product {
  id: ID!
  artist: String!
  title: String!
  description: String!
  price: Int!
  coverImg: String!
  genre: String!
  format: String!
}